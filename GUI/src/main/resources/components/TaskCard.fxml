<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.control.Label?>
<?import javafx.scene.layout.BorderPane?>
<?import javafx.scene.layout.HBox?>
<?import javafx.scene.layout.Region?>
<?import javafx.scene.layout.VBox?>
<?import javafx.scene.layout.GridPane?>
<?import javafx.scene.layout.ColumnConstraints?>
<?import javafx.scene.layout.RowConstraints?>

<fx:root type="javafx.scene.layout.BorderPane"
         xmlns="http://javafx.com/javafx/21"
         xmlns:fx="http://javafx.com/fxml/1">

    <!-- PADDING GENERALE -->
    <padding>
        <Insets top="15" right="15" bottom="15" left="15"/>
    </padding>

    <!-- DATE -->
    <top>
        <Label fx:id="dateLabel"
               styleClass="task-date-lbl"
               wrapText="true"
               alignment="CENTER"
               textAlignment="CENTER"
               maxWidth="550"/>
    </top>

    <center>
        <VBox alignment="TOP_CENTER"
              spacing="20"
              maxWidth="600"
              prefWidth="600"
              BorderPane.alignment="CENTER">

            <padding>
                <Insets top="20" right="20" bottom="20" left="20"/>
            </padding>

            <!-- ===== TITLE SECTION ===== -->
            <VBox alignment="CENTER" spacing="10">

                <Label fx:id="titleLabel"
                       styleClass="task-description-lbl"
                       wrapText="true"
                       maxWidth="550"
                       alignment="CENTER"
                       textAlignment="CENTER"/>

                <Label fx:id="stateLabel"
                       styleClass="task-state-lbl"
                       wrapText="true"
                       alignment="CENTER"
                       textAlignment="CENTER"/>

            </VBox>

            <!-- ===== BOTTOM SECTION ===== -->
            <VBox alignment="CENTER" spacing="25">
                <HBox alignment="CENTER_RIGHT">
                    <GridPane fx:id="workersGrid"
                              hgap="15"
                              vgap="15"
                              maxWidth="300">

                        <columnConstraints>
                            <ColumnConstraints percentWidth="50"/>
                            <ColumnConstraints percentWidth="50"/>
                        </columnConstraints>

                        <rowConstraints>
                            <RowConstraints percentHeight="100"/>
                        </rowConstraints>

                    </GridPane>

                </HBox>

                <HBox alignment="CENTER" spacing="20">

                    <Button fx:id="acceptButton"
                            text="Accept task"
                            styleClass="styled-btn"/>

                    <Region HBox.hgrow="ALWAYS"/>

                    <Button fx:id="changeStateButton"
                            styleClass="styled-btn"/>

                </HBox>

            </VBox>

        </VBox>
    </center>
</fx:root>
